<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="logistics.work.models.dao.UserDao">
    <select id="queryDeptNumberByUserId" parameterType="Integer" resultType="String">
        select
        dept_number
        from employee
        where id=(select employee_id from `user`where `user`.id=#{userId})
    </select>
    <select id="queryEmployeeByDeptNumber" parameterType="String" resultType="logistics.work.models.domain.Employee">
select
`user`.id,
employee.id as employeeId,
employee.emp_name,
employee.emp_job,
employee.dept_number
from employee
left join `user`
on `user`.employee_id=employee.id
where employee.dept_number=#{deptNumber} and employee.emp_job not like '经理'
    </select>
    <select id="queryAllDept" resultType="logistics.work.models.dto.DeptDto">
        select *
        from department
    </select>
    <select id="queryAllEmployee" resultType="logistics.work.models.domain.Employee">
select
`user`.id,
employee.id as employeeId,
employee.emp_name,
employee.emp_job,
employee.dept_number
from employee
left join `user`
on `user`.employee_id=employee.id
    </select>
</mapper>